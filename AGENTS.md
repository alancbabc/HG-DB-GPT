# AGENTS.md

## 项目定位

- 本项目当前仍是通用 Agent 平台。
- 前端 UI 的配置化隐藏改造已基本完成，现有可见性配置和已确认的隐藏结果需要继续保持。
- 下一阶段目标是在保留现有业务实现和核心功能可用性的前提下，完成无法联网的 Windows 产线环境部署准备。
- 产线目标是在本机浏览器中使用本项目，通过 Ollama 或其他本地推理服务调用离线大语言模型和 Embedding 模型，并支持本地 SQLite 数据库、文档、知识库、数据分析查询和定时任务。
- 产线 SQLite 数据库由外部 C++ 软件持续写入；本项目只能以只读方式连接和查询，并需要兼顾实时写入期间的并发稳定性。
- 后续改造不删除实际业务逻辑，不收窄通用 Agent 平台的后端能力，也不改变主架构；产线部署配置可以只开放已确认需要的前端入口。
- 此前将项目限定为“只读数据库查询分析助手”的判断无效。

## 当前阶段与优先级

当前阶段从“UI 盘点与隐藏”转为“离线 Windows 产线化”。工作优先级如下：

1. P0：实现并验证生产 SQLite 的真实只读连接；
2. P0：形成可在断网 Windows 电脑上重复安装的完整离线部署包；
3. P0：保留 Agent 现有 Python、Shell 和文件系统能力，通过 `LocalService` 与 NTFS ACL 防止其写入生产数据库；
4. P1：完成 Ollama 或其他本地推理服务、LLM、Embedding 模型的离线导入、启动、健康检查和故障提示；
5. P1：增加 SQL 查询行数、结果大小、执行时间和并发限制，验证与 C++ 写入程序并行运行；
6. P1：完成 DB-GPT 与模型服务的 Windows 服务化、自启动、日志、备份和恢复方案；
7. P1：验证文档、知识库、数据分析和定时任务在完全断网环境中的完整链路；
8. P2：完善监控、容量规划、升级、回滚和长期稳定性测试。

在 P0 项完成前，不得将当前版本判断为可直接投入生产。

## 决策边界

- 哪些前端页面、菜单、按钮、卡片或功能入口需要隐藏，最终由用户决定。
- Agent 可以提出隐藏候选项和建议，但必须标记为“待确认”，不能替用户作决定。
- 信息不足或依赖不明确时，默认保留并继续调查。
- 未经用户明确确认，任何功能默认保持可见。
- 不删除前后端业务实现、API、路由、组件注册、后台任务、依赖、数据模型、迁移或历史数据。
- 隐藏只用于控制前端呈现和用户入口，不等于删除能力，也不应被当作权限控制或安全边界。
- 除非用户另行明确授权，不通过关闭后端服务、注销组件或改变接口行为来实现前端隐藏。
- 不以“代码复杂”或“暂时看不到用途”作为隐藏功能的单独理由。
- 除非用户另行要求，不增加产品功能，不进行大规模重构或产品方向收窄。
- 离线部署、安全加固、真实只读连接和稳定性改造可以改变基础设施层和连接层实现，但必须保持现有业务语义，并通过独立测试证明不会破坏通用平台能力。
- 生产数据库只读不是 UI 隐藏问题，不能通过提示词、按钮隐藏或 SQL 关键词黑名单代替底层只读连接和操作系统权限。
- 任何数据库迁移、清库、删除向量数据、删除上传文件、修改生产数据库 journal mode 或调整 C++ 写入程序，都必须明确目标和影响后再执行。

## 离线 Windows 部署原则

- 目标环境默认完全无法访问互联网；运行、启动、模型调用、文档解析、知识库检索和定时任务不得依赖在线下载或云端回退。
- 前端静态资源应在联网构建环境提前构建并随发布包交付，产线环境不要求安装 Node.js 或执行前端构建。
- 不直接复制开发机 `.venv` 作为部署方案。应固定 Python 版本和 Windows 架构，准备完整 wheelhouse，并使用 `--no-index`、`--find-links` 等离线方式安装。
- 离线包至少包括：应用代码或 wheels、全部 Python 依赖、Ollama 或替代推理服务、LLM、Embedding 模型、必要的系统运行库、启动脚本、服务配置、校验清单和验收脚本。
- 所有会在运行时尝试联网的功能都需要识别并记录；产线配置应关闭或隐藏 URL 知识库、在线模型下载、插件/技能市场、云模型和其他未经确认的联网入口。
- DB-GPT、Ollama、模型、元数据库、向量库、上传文件、生成结果和日志使用明确的绝对路径。可变运行数据不得放在源码目录、Git 跟踪目录或升级时会被覆盖的位置。
- 默认仅供本机浏览器使用时，Web 和 Ollama 应监听回环地址。若需要局域网访问，必须单独设计认证、授权、TLS、防火墙和访问审计。
- DB-GPT 和 Ollama 应以 Windows 服务或等价的受控后台方式运行，具备开机启动、失败恢复、受控停止、日志轮转和健康检查。
- 所有发布物应固定版本。哈希只用于发布包关键安装文件在制作和移动介质复制后的必要校验，不反复哈希模型、wheelhouse、日志、缓存和普通备份文件；升级前备份 DB-GPT 元数据库、向量数据、任务数据和用户文件，并验证回滚。
- Windows第三方运行时已固定为Python 3.11.9 x64、Ollama 0.32.15、NSSM 2.24-101-g897c7ad和Microsoft Visual C++ x64 14.50.35719.0；版本更新必须先重新完成真实介质与断网验收。
- Python离线介质使用 `scripts/windows/offline_media.py` 从当前工作区和 `uv.lock` 生成；组包前必须通过 `Test-OfflinePythonMedia.ps1` 的全新临时环境 `--no-index` 安装、`pip check` 和运行时自检。目标机不得接收sdist或现场编译依赖。

## 本地模型与 Embedding 原则

- LLM 与 Embedding 是两条独立的必需链路，必须分别准备、验证和监控。
- 在 DB-GPT 页面“添加模型”只代表创建或启动模型配置，不应假定它会在离线环境自动获得模型权重。
- Ollama 模型应在联网准备环境提前下载，或在产线通过随包交付的本地 GGUF/Safetensors 和 Modelfile 导入；不得在产线依赖 `ollama pull`。
- 配置中的模型名称必须与本地推理服务实际注册的名称一致；启动前检查本地 API、模型列表、生成接口和 Embedding 接口。
- 使用 Ollama provider 时，离线 Python 依赖必须显式包含项目的 `proxy_ollama` 可选依赖及其锁定版本。
- 模型选型不能只验证普通问答，还要验证中文字段理解、SQLite SQL 生成、工具调用、结构化输出、长表结构上下文、文档检索、定时任务和错误恢复。
- 模型大小、量化、上下文长度、并发数和常驻时间必须根据产线 CPU、内存、GPU/显存和任务延迟要求实测确定；未经实测不承诺性能。
- 禁止配置不可用的云端 fallback；离线环境中的模型不可用应快速失败并给出明确诊断。
- 已确认主 LLM 为 `qwen3.5:27b-q4_K_M`，备用 LLM 为 `qwen3.5:9b-q4_K_M`，Embedding 为 `qwen3-embedding:0.6b`。干净断网测试机使用 RTX 5090，最终产线性能在 RTX 3090 24 GB 上复测。
- 模型介质使用Ollama原生 `manifests` 与 `blobs` 仓库，发布前用 `ollama_model_store.py` 校验三个固定标签、blob存在性和大小，不对大模型重复计算哈希。产线Ollama设置 `OLLAMA_NO_CLOUD=1`，默认单模型、单路推理和8192上下文，只有3090实测通过后才能提高并发或上下文。
- 三个固定模型已在RTX 5090开发机完成真实下载与生成/Embedding接口验收；该结果只证明介质和调用链可用，不能替代RTX 3090产线机的显存、延迟和稳定性验收。
- 本机断网验收不得关闭物理网卡或影响远程控制；使用仅绑定候选包Python、Ollama和llama-server路径的可回滚Windows防火墙规则阻断Internet，并将结果标记为进程级隔离验收。
- 2026-08-27已在RTX 5090本机完成进程级Internet隔离安装验收：DB-GPT/Ollama服务、回环Web健康、27B/9B生成和1024维Embedding均通过；未关闭物理网络。该结果不替代干净系统物理断网验收，也不替代RTX 3090产线性能复测。
- 第一份26.86GB完整候选离线发布目录已在准备机完成清单校验和两轮 `--no-index` Python安装自检；发布物不进入Git，仍需在干净Windows测试机完成物理断网全新安装后才能升级结论。
- 干净测试机使用 `Test-DBGPTOfflineInstallation.ps1` 统一验收两个Windows服务、物理断网、回环监听、Web健康、模型仓库及三模型真实接口；准备机模拟或仅设置 `OLLAMA_NO_CLOUD=1` 不能替代该验收。

## 生产 SQLite 只读与并发原则

- 生产数据库连接必须使用 SQLite URI `mode=ro` 或等价的底层只读打开方式，不能继续使用普通可写 `sqlite:///path` 连接。
- 每个连接应增加 `PRAGMA query_only=ON` 作为纵深保护，但不能把它当作唯一安全边界。
- DB-GPT 使用独立的低权限 Windows 服务账号；该账号对生产数据库及其目录只拥有读取所需权限，不得拥有修改、删除、重命名或替换生产数据库的权限。
- 数据源路径不存在、不可读或指向目录时必须失败，不能自动创建同名数据库、父目录或空文件。
- SQL 工具只允许单条只读 SELECT/CTE 查询，显式禁止 DML、DDL、PRAGMA、ATTACH、DETACH 及其他可能改变数据库或连接状态的语句。简单的开头关键词黑名单不视为充分校验。
- 所有查询必须有默认和最大行数、最大返回字节数、最大执行时间及可取消机制；不能先 `fetchall()` 全部数据后仅截断 UI 输出。
- 大表查询、定时任务和并行 Agent 查询需要全局并发限制，防止耗尽内存、阻塞 C++ 写入或导致 WAL 文件持续增长。
- 推荐由 C++ 写入端评估并管理 WAL、busy timeout、事务长度和 checkpoint；DB-GPT 只读端不得擅自改变生产数据库 journal mode。
- 生产数据库及 `-wal`、`-shm` 文件应位于同一台 Windows 电脑的本地磁盘，不把 WAL 数据库放在不可靠的网络共享目录。
- 不使用 `immutable=1` 读取仍在实时变化的数据库。
- 必须与真实 C++ 写入程序执行一小时并发测试，覆盖正常写入、长查询、宿主停止、恢复重连和数据库完整性；不再设置24/72小时验收门槛。

## Agent 执行与宿主机安全原则

- 当前 Python 代码解释器和本地 Shell 运行时均可能在宿主机进程中执行模型生成的代码；超时、工作目录或字符串检查不等于安全沙箱。
- 生产方案应优先使用边界明确的 SQL、文件读取、统计和图表工具。是否保留任意 Python/Shell 能力由用户确认，并作为独立安全决策记录。
- 已确认保留代码执行；DB-GPT使用 `LocalService`，通过NTFS ACL禁止其修改、删除、重命名或替换生产数据库，并限制对应用密钥和无关目录的访问。该方案不是代码沙箱，剩余风险必须在目标机权限验收中记录。
- 文档内容、数据库内容和模型输出都视为不可信输入，需考虑提示注入、路径穿越、命令执行、资源耗尽和敏感信息泄露。
- UI 隐藏不是授权控制；被隐藏的路由、API 和工具仍需依靠后端鉴权、配置、进程权限和操作系统 ACL 保护。

## 定时任务产线原则

- 定时任务定义、执行历史和关联文件必须持久化，服务重启后验证已启用任务能够恢复。
- 明确电脑关机、服务停止、错过触发时间、时区变化和系统时间校准时的补跑或跳过策略。
- 除单任务 `max_instances` 外，还需根据模型和数据库承载能力设置全局任务并发、队列上限和背压策略。
- 外层任务超时必须能实际中断模型请求、SQL 查询和子进程，不能只更新任务状态后留下后台工作继续运行。
- 定时任务应记录开始时间、结束时间、模型、数据库、输入资源、结果、失败原因和重试情况，并设置历史、对话、生成文件和日志的保留策略。
- 任务触发前应检查 DB-GPT、模型服务、目标模型、Embedding、生产数据库和磁盘空间的健康状态；依赖不可用时快速失败并保留可诊断信息。

## 配置化隐藏原则

- 使用集中、可审计的功能可见性配置作为唯一事实来源，避免把开关散落为页面内硬编码条件。
- 每个可隐藏功能使用稳定、语义明确的配置键，并记录默认值、控制范围、前端入口和依赖关系。
- 配置默认值应保持现有功能可见，避免升级或缺失配置时意外隐藏功能。
- 导航、页面入口、按钮、卡片及相关前端交互应读取同一配置，避免出现入口隐藏但其他位置仍残留的情况。
- 隐藏状态必须可通过修改配置恢复，不要求恢复代码、重新安装依赖或迁移数据。
- 若某项 UI 与核心链路共用组件、状态、API 或初始化流程，应优先只隐藏展示层，不能破坏共享能力。
- 若隐藏某项入口会影响 Agent 对话、模型调用、数据源、知识库、文件、会话、结果渲染等核心链路，应暂停实施并向用户说明影响。
- 是否禁止通过直接 URL 访问已隐藏页面，需要作为独立行为向用户确认；不能把“隐藏入口”自动扩大为“关闭路由”。

## 已完成阶段：功能分析与 UI 隐藏

以用户可感知的完整功能为单位分析，不按目录名直接下结论。

每项功能至少说明：

| 项目 | 内容 |
| --- | --- |
| 功能与入口 | 页面、API、CLI 或后台任务 |
| 主要代码 | 前端、后端、配置和数据模型位置 |
| 依赖关系 | 依赖什么、被什么依赖 |
| 平台作用 | 与通用 Agent 核心链路的关系 |
| 当前状态 | 主链路、兼容、可选、示例、实验或疑似废弃 |
| 成本与风险 | 启动、依赖、维护、测试、安全和兼容成本 |
| 候选意见 | 保持可见或候选隐藏 |
| 配置建议 | 建议的配置键、默认值和控制范围 |
| 隐藏影响 | 对共享组件、核心链路和直接访问方式的影响 |
| 恢复方式 | 如何仅通过配置恢复可见性 |
| 结论状态 | 待用户确认或已确认 |

分析结论可按以下类别整理：

- 核心候选：通用 Agent 平台及主要用户链路可能必需；
- 支撑候选：被核心功能依赖的基础能力；
- 重复候选：旧新链路或多个实现解决同一问题；
- 可选候选：只用于特定部署、模型或场景；
- 示例/实验候选：Demo、示例 DAG、实验页面或开发工具；
- 待确认隐藏候选：已有证据表明不是当前所需入口，且隐藏不影响核心功能；
- 未知：证据不足，默认保留。

候选意见只是分析结果。用户确认前，不实施隐藏。

## 用户确认后的隐藏流程

用户明确确认某项前端功能需要隐藏后，按以下顺序实施：

1. 补充现状测试和完整依赖清单；
2. 明确集中配置键、默认值、控制范围和恢复方式；
3. 让相关导航、页面入口和 UI 控件统一读取配置；
4. 分别验证配置开启与关闭两种状态；
5. 验证通用 Agent 核心流程、相关测试和前端构建；
6. 检查后端实现、API、组件注册、依赖和数据结构仍保持完整；
7. 记录实际隐藏项和未验证风险。

每次只处理一个边界清晰的功能或一组强相关入口。不得在隐藏改动中夹带代码删除、依赖卸载或无关重构。

## 工程与版本管理

- 修改前检查 `git status`，保留用户已有改动。
- 使用 `rg` 查找入口、引用、路由、组件注册、配置和测试。
- 不使用 `git reset --hard`，不大范围覆盖工作区。
- 不提交密钥、本地配置、日志、缓存、数据库文件和 `.venv`。
- 分析、集中配置建设和各组 UI 隐藏应保持边界清晰，不混入无关重构。
- 离线部署清单、通用安装脚本、只读连接实现和测试可以提交；机器专用路径、生产模型文件、生产数据库、用户上传、运行数据和服务账号凭据不得提交。
- 修改 SQLite 连接器、SQL 校验、代码执行或调度器前，先补充能够复现当前风险的测试；修改后同时验证安全关闭状态和正常功能状态。
- 所有部署测试明确记录 Windows 版本、Python 版本、CPU、内存、GPU/驱动、模型、量化、SQLite journal mode 和 C++ 写入负载。
- 修改后运行与风险匹配的测试，并说明未验证部分。

## 当前首要产出

按优先级形成并实施以下产出：

1. 离线 Windows 部署架构、目录规划、服务账号和数据权限方案；
2. SQLite 真实只读连接、只读 SQL 校验、查询资源限制及对应测试；
3. C++ 实时写入与 DB-GPT 查询的 WAL/锁/性能联合验证方案；
4. LLM 与 Embedding 的离线准备、导入、配置、健康检查和模型验收方案；
5. 完整 wheelhouse、模型、系统依赖、前端静态资源、安装脚本和精简关键文件校验清单组成的离线发布包；
6. DB-GPT 与 Ollama 的 Windows 服务化、自启动、停止、恢复、日志和备份方案；
7. 保留 Agent 代码执行能力时的 `LocalService`、NTFS ACL和生产数据库拒写测试；
8. 数据库、文档、知识库、数据分析、定时任务、断网、重启、升级和回滚的产线验收清单。

后续每项实施仍需边界清晰，不在安全或部署改动中夹带业务删除和无关重构。若发现会影响现有可见功能、C++ 写入稳定性、生产数据完整性或离线运行，应立即停止相关变更并向用户反馈。
